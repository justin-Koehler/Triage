apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: critr

resources:
  - namespace.yaml
  - configmap.yaml
  - postgres.yaml
  - app.yaml
  - ingress.yaml
  - networkpolicy.yaml

# Secret bewusst nicht hier — aus secret.example.yaml oder kubectl create.
# images:
#   - name: critr
#     newTag: "abc1234"
